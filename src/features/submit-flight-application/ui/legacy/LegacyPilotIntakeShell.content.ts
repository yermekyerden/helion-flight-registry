export const legacyPilotIntakeShellContent = {
  title: 'Legacy terminal protocol',
  description:
    'This intake flow will use an uncontrolled form. The terminal accepts the submitted packet first and reports validation errors after the operator attempts to submit the dossier.',
} as const;
